fastapi>=0.95
uvicorn[standard]>=0.22
SQLAlchemy>=2.0
alembic>=1.10
pydantic>=2.0
python-jose[cryptography]>=3.3
passlib==1.7.4
# SQLite async driver for the default local quickstart
aiosqlite>=0.18.0
# Pydantic v2 settings compatibility
pydantic-settings>=0.1
# Email validator used by Pydantic's EmailStr
email-validator>=1.3.0
psycopg[binary]>=3.1
redis>=4.5

# Optional heavy deps for AI / imaging (install separately in appropriate env/runtime)
# torch
# monai
# opencv-python
